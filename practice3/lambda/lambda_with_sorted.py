# Here is a list of names.
names = ["Milana", "Alexander", "Bob", "Ivan"]

# Here is sorted() sorting names by their length.
sorted_names = sorted(names, key=lambda name: len(name))
print("Names sorted by length:", sorted_names)
